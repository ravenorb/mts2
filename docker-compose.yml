services:
  mts2-web:
    build:
      context: .
      dockerfile: Dockerfile
    image: mts2-web:local
    container_name: mts2-web
    ports:
      - "8080:8080"
    environment:
      ASPNETCORE_ENVIRONMENT: Development
    restart: unless-stopped
